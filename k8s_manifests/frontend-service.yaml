apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: student-management
  labels:
    app: frontend
spec:
  type: LoadBalancer        # Exposes a public IP via GKE
  selector:
    app: frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
